import{createServer}from'node:http';import{readFile,stat}from'node:fs/promises';import{resolve,extname,join}from'node:path';const base=resolve(process.argv[2]||'.');const port=Number(process.env.PORT||4173);const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json'};createServer(async(req,res)=>{try{const path=decodeURIComponent(new URL(req.url,'http://localhost').pathname);let file=resolve(base,'.'+path);if(!file.startsWith(base))throw Error('bad path');try{if((await stat(file)).isDirectory())file=join(file,'index.html')}catch{}const body=await readFile(file);res.writeHead(200,{'content-type':types[extname(file)]||'application/octet-stream','cache-control':'no-store'});res.end(body)}catch{try{res.writeHead(200,{'content-type':types['.html']});res.end(await readFile(join(base,'index.html')))}catch{res.writeHead(404);res.end('Not found')}}}).listen(port,'0.0.0.0',()=>console.log(`S&OP Consensus Agent available on http://localhost:${port} (serving ${base})`));
